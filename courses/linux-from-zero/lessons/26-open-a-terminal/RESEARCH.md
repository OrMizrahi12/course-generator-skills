# Lesson 26 — Open a terminal and run a command

Locked to this lesson only. Do not re-plan the syllabus. Do not research lesson 27 here.

## Feature

Open a terminal emulator from the desktop, type a command, press Return, and read the computer’s real output.

## What it is / is not

- It is: xfce4-terminal 1.1.3 as **Terminal Emulator** on this host’s right-click applications menu. Type a UNIX command at the prompt; Return runs it.
- It is not: launching the terminal from a script (`film_lesson` must not spawn `xfce4-terminal` off the menu). It is not `echo`. It is not reading the prompt (lesson 27).

## Live sources (fetched this pass)

- Xfce Docs: Getting Started — start Terminal from the desktop applications menu; the window shows a command prompt; type a UNIX command and press Return. Docs still say {gui>System>Xfce Terminal}; **this host’s live menu lists Terminal Emulator as a top-level item.** Film the live menu. https://docs.xfce.org/apps/xfce4-terminal/getting-started
- Xfce Docs: xfce4-terminal start — lightweight VTE terminal. https://docs.xfce.org/apps/xfce4-terminal/start
- Ubuntu Noble `date(1)` from GNU coreutils 9.4 — print the system date and time. https://manpages.ubuntu.com/manpages/noble/man1/date.1.html

## Live operation on this host (2026-08-21)

- Right-click wallpaper → **Terminal Emulator** (second item, under Run Program…). Hover at 740,448 highlights it.
- Window opens around 1502×1022 — not full-bleed. Fullscreen it before it is the subject.
- Font already JetBrains Mono 19 (1.75× default). Filming setup, not on-camera.
- `date` → `Fri Aug 21 08:00:35 PM UTC 2026` (time will differ on take). `date --version` → GNU coreutils 9.4.
- `rent-receipts` exists from lesson 25. Do not recreate it. Do not start `~/linux-workshop`.

## Human job

A person made a `rent-receipts` folder and needs today’s date to write on a receipt before scanning it. Opening a terminal and running `date` is required; a wall clock is not on this desktop.

## Done on screen

Fullscreen terminal, command `date` typed, output a full date line including `2026`, prompt back.

## Terminal font

JetBrains Mono 19 already set (1.75× default).

## Workshop

Still starts at lesson 35.

## Viewer must see created on camera

Opening Terminal Emulator from the desktop menu, the empty prompt, typing `date`, and the printed date. Do not pre-run `date` as the lesson object.
